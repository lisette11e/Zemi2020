using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToSp1OP : MonoBehaviour
{
    // Start is called before the first frame update
    void ToSt1OP()
    {
        FadeManager.Instance.LoadScene("St1op", 2.0f);
    }
}
